import { Card } from 'react-bootstrap';
const CommentCard = ({ comment }) => {
  
  return ( 
    <li>
      <Card>
        <Card.Header>
          {comment.title}
        </Card.Header>
        Card.ImgOverlay, Card.Header, Card.Body, ListGroup, etc

      </Card>
    </li>
  )

}

export default CommentCard;